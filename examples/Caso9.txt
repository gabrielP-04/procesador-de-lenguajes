function int procesarDatos(string mensaje, int cantidad) {
    var boolean resultado;
    var int total;

    resultado = mensaje || total;
    resultado = mensaje == 'hola'; 
    total = resultado + cantidad; 

    output resultado;             
  
    return mensaje;               
}
