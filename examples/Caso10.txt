function boolean verificarAcceso(int nivel) {
    var string mensaje;
    var int clave;
    var boolean result;

    mensaje = 5;
    clave = '1234';

    if (nivel == 3)
        output 'Acceso permitido';
}

return;

function void iniciarSistema(void) {
    var int nivel;
    nivel = 3;

    while (nivel){
        verificarAcceso(); 
        nivel = nivel + 1;
        return;
    }

    return nivel; 
}

iniciarSistema();