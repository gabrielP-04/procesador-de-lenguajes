var string userName;
var boolean isAdmin;

userName = 'Admin';
isAdmin = true;

function void greetUser(string name) {
  while (isAdmin) {
    output('Welcome, ' + name);
    isAdmin = false; 
  }
}

greetUser(userName);