var string userName;
var boolean isAdmin;

userName = 'Admin';

function void greetUser(string name) {
  while (isAdmin) {
    output('Welcome ');
    output(name);
  }
}

greetUser(userName);